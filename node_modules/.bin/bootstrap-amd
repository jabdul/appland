#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../bootstrap-amd/bootstrap-amd.js" "$@"
  ret=$?
else 
  node  "$basedir/../bootstrap-amd/bootstrap-amd.js" "$@"
  ret=$?
fi
exit $ret
