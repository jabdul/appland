#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../jamjs/bin/jam.js" "$@"
  ret=$?
else 
  node  "$basedir/../jamjs/bin/jam.js" "$@"
  ret=$?
fi
exit $ret
